<Window x:Class="ApiStudy.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ApiStudy"
        mc:Ignorable="d"
        Title="공공데이터 게시판" Height="500" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 상단: 조회 버튼 -->
        <Button Grid.Row="0"
                Content="조회"
                Command="{Binding ApiResponseCommand}"
                HorizontalAlignment="Left"
                Padding="20,5"
                Margin="0,0,0,10"/>

        <!-- 중단: 콤보박스 (제목 선택) -->
        <ComboBox Grid.Row="1"
                  ItemsSource="{Binding Items}"
                  SelectedItem="{Binding SelectedItem}"
                  DisplayMemberPath="Title"
                  Margin="0,0,0,10"/>

        <!-- 하단: 선택된 항목 상세 -->
        <Border Grid.Row="2"
                BorderBrush="Gray"
                BorderThickness="1"
                Padding="15"
                CornerRadius="4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="ID" FontWeight="Bold" Margin="0,0,0,8"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedItem.Id}" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="작성일" FontWeight="Bold" Margin="0,0,0,8"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedItem.Written_Dt, StringFormat=yyyy-MM-dd}" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="제목" FontWeight="Bold" Margin="0,0,0,8"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedItem.Title}" TextWrapping="Wrap" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="내용" FontWeight="Bold"/>
                <ScrollViewer Grid.Row="3" Grid.Column="1" VerticalScrollBarVisibility="Auto">
                    <TextBlock Text="{Binding SelectedItem.Txt_Origin_cn}" TextWrapping="Wrap"/>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</Window>
